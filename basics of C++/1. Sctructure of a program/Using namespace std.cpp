// my second program in C++
#include <iostream>
using namespace std;
/* The above declaration allows all elements in the std namespace to be
accessed in an unqualified manner (without the std:: prefix).
With this in mind, the last example can be rewritten to make
unqualified uses of cout as:*/
int main ()
{
  cout << "Hello World! ";
  cout << "I'm a C++ program";
}
